#include <iostream>
#include <fstream>
#include <string>

using namespace std;

int main()
{
    fstream in_file("input.txt");
    string line;
    while (getline(in_file, line)) {
        // 
    }
    int count;
    return count;
}
